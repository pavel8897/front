export const Advantage = ({value, label}) => {
    return (
        <div className="advantage">
           <div className="value">{value}</div>
           <div className="label">{label}</div>
        </div>
    )
}